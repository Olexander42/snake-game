const root = document.documentElement;

const html = document.querySelector("html");

const scoreEl = document.querySelector(".score");
const recordEl = document.querySelector(".record");

export { root, html, scoreEl, recordEl };